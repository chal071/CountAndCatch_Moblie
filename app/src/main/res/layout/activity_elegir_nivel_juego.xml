<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bgelegirnivel">

    <LinearLayout

        android:id="@+id/buttonsLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="110dp"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        >

        <Button
            android:id="@+id/btnJcFacil"
            android:layout_width="400dp"
            android:layout_height="560dp"
            android:layout_margin="10dp"
            android:background="@drawable/dificil"
            app:backgroundTint="@null" />

        <Button
            android:id="@+id/btnJcIntermedio"
            android:layout_width="400dp"
            android:layout_height="560dp"
            android:layout_margin="10dp"
            android:background="@drawable/medio"
            app:backgroundTint="@null" />

        <Button
            android:id="@+id/btnJcDificil"
            android:layout_width="400dp"
            android:layout_height="560dp"
            android:background="@drawable/facil"
            app:backgroundTint="@null" />
    </LinearLayout
        >

</LinearLayout>
